# 优化目标计划 — 业务链路完整化与本地 ASR 优化

> 日期：2026-07-07  
> 前提：完整真实链路已验证（真实 ASR + 真实 codexai LLM → 真实卡片+纪要+方案考量）  
> 用户最看重：**实时会议** + **录音文件转换** 两个场景

## 现状（诚实）
- ✅ 完整真实链路跑通：wav→sherpa sidecar→session→真实 LLM→卡片+纪要（real_e2e_real_llm.py 验证）
- ✅ 本地 ASR 达门禁：FunASR batch+归一化 recall 0.86 > 0.8
- ⚠️ 实时 ASR 用 sherpa（准度 0.43 低），FunASR batch（0.86 达标）没接成 sidecar
- ❌ 录音文件转换（批量）完全没做——无"上传音频文件→转写→卡片+纪要"端点
- ❌ 会前设备检查、工程语境门禁、JSON 导出 漏做

## 目标计划（按价值排序）

### G1 — 录音文件转换（批量，最高价值）⭐
- POST /live/asr/transcribe-file/sessions：上传音频文件 → FunASR batch 转写（更准，0.86）→ session → 卡片+纪要
- 用 FunASR batch（paraformer-zh）subprocess 转写，复用 transcribe_funasr.py
- 自测：上传 simulated-release-review.16k.wav → 转写 → session → 真实 LLM 卡片+纪要

### G2 — 实时会议 ASR 优化
- 当前实时用 sherpa（0.43）。接 FunASR streaming sidecar（更准）或实时 sherpa + 文件 FunASR batch 分流
- 让实时管线用上达标的 ASR
- 自测：实时 WS 流 recall 提升

### G3 — 会前设备检查
- GET /audio/check：mic 电平/权限/设备检查（product-requirements 会前验收点：30s 内确认音频输入正常）
- 自测：返回设备列表+电平

### G4 — 工程语境门禁
- web backend 加 is_engineering_meeting gate（非工程会议工程卡片为 0，product-requirements 验收点）
- 自测：非工程转写不产工程卡片

### G5 — JSON 导出
- 纪要 JSON 格式（接 Jira/Linear/GitHub，product-requirements §5）
- 自测：JSON 结构含决策/待办/风险/证据

## 执行顺序
G1（录音文件转换）→ G2（实时 ASR 优化）→ G3（设备检查）→ G4（语境门禁）→ G5（JSON 导出）
每个：实现 → 自测（真实组件）→ commit。
