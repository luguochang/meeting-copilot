# PC Local Web MVP

Local PC Web MVP for Meeting Copilot.

Current backend scope:

- `GET /`
- `GET /static/app.js`
- `GET /static/styles.css`
- `GET /health`
- `GET /desktop/shell-readiness`
- `GET /desktop/runtime-boundary`
- `GET /desktop/native-bridge-contract`
- Web-only `desktop-native-runtime-panel` that calls Tauri no-op IPC only when `window.__TAURI__` is available
- `GET /demo/fixtures`
- `POST /demo/fixtures/{fixture_id}/sessions`
- `POST /sessions`
- `GET /sessions/{session_id}`
- `GET /sessions/{session_id}/events`
- `GET /sessions/{session_id}/events.sse`
- `POST /live/mock/fixtures/{fixture_id}/sessions`
- `GET /live/sessions/{session_id}/events`
- `GET /live/sessions/{session_id}/events.sse`
- `POST /live/asr/mock/sessions`
- `GET /live/asr/sessions/{session_id}/events`
- `GET /live/asr/sessions/{session_id}/events.sse`
- `GET /live/asr/sessions/{session_id}/suggestion-candidates`
- `GET /live/asr/sessions/{session_id}/llm-request-drafts`
- `GET /live/asr/sessions/{session_id}/llm-openai-request-body-previews`
- `POST /live/asr/sessions/{session_id}/llm-schema-validation-dry-runs`
- `POST /live/asr/sessions/{session_id}/llm-card-creation-policy-dry-runs`
- `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-preview-dry-runs`
- `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-preflight-dry-runs`
- `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-runs`
- `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-repository-dry-runs`
- `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-transaction-runs`
- `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-result-audit-previews`
- `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-retry-replay-preflights`
- `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-event-serializer-dry-runs`
- `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-mutation-preflights`
- `GET /live/asr/sessions/{session_id}/llm-execution-previews`
- `POST /live/asr/sessions/{session_id}/llm-execution-runs`
- `GET /live/asr/sessions/{session_id}/llm-provider-readiness`
- `GET /live/asr/sessions/{session_id}/llm-provider-config-boundary`
- `GET /live/asr/sessions/{session_id}/llm-provider-masked-status`
- `POST /live/asr/sessions/{session_id}/llm-provider-config-validation`
- `POST /live/asr/sessions/{session_id}/llm-provider-config-loader-preflight`
- `GET /live/asr/sessions/{session_id}/llm-provider-secret-storage-policy`
- `POST /live/asr/sessions/{session_id}/llm-provider-config-reader-dry-run`
- `POST /live/asr/sessions/{session_id}/llm-provider-masked-status-loader-dry-run`
- `GET /live/asr/sessions/{session_id}/draft`
- `GET /live/asr/sessions/{session_id}/draft.md`
- `PATCH /sessions/{session_id}/cards/{card_id}/status`
- `GET /sessions/{session_id}/report.md`
- `DELETE /sessions/{session_id}`

Run tests:

```bash
cd code/web_mvp/backend
pytest -q
```

Run the PC Web MVP quality gate from the repo root:

```bash
cd /Users/chase/Documents/面试/meeting-copilot
python3 tools/run_quality_gate.py --profile pc-web
```

The quality gate runs root scaffold contract tests, core tests, backend tests, and the browser smoke. It does not call remote ASR, the LLM gateway, or `configs/local/`.

Since PCWEB-082, the repo-level `pc-web` quality gate also runs `root-pytest` before core/backend/browser. These tests statically check `code/desktop_tauri/src-tauri/tauri.conf.json`, the minimal Tauri v2 scaffold, and the no-op bridge handlers `runtime_get_status`, `session_prepare`, and `asr_worker_health`. They do not run `cargo build`, `cargo tauri dev`, `npm install`, or any package manager, and they forbid `Cargo.lock`, `package.json`, dependency lock files, installer artifacts, generated build directories, audio capture, worker spawn, secret reads, and paid provider calls.

PCWEB-083 adds `code/desktop_tauri/build-readiness.policy.json` and `tools/desktop_build_readiness.py`. The policy keeps `safe_to_run_cargo_check_now=false` and the tool is static by default; when explicitly requested it only performs `toolchain_version_probe_only` with `rustc --version` and `cargo --version`. The executable probe allowlist is enforced in code, so a custom policy cannot expand the runnable command set. It still does not run `cargo check`, Tauri dev/build, dependency installation, lockfile generation, audio capture, worker spawn, secret reads, local runtime writes, or remote provider calls.

PCWEB-084 adds `code/desktop_tauri/cargo-check.policy.json` and `tools/desktop_cargo_check_policy.py`. The policy decides the first future `cargo check` artifact boundary without running Cargo: the first explicitly approved dependency-resolution run may generate `code/desktop_tauri/src-tauri/Cargo.lock`, build output must use `CARGO_TARGET_DIR=artifacts/tmp/desktop_tauri_target`, and repeat checks should prefer `--locked --offline` after lock/cache exist. It keeps `safe_to_run_cargo_check_now=false` and still does not install Rust, fetch dependencies, run Cargo/Tauri/package managers, capture audio, spawn workers, read secrets, read `configs/local/`, or call remote providers.

PCWEB-085 adds `code/desktop_tauri/rust-toolchain-readiness.policy.json` and `tools/desktop_rust_toolchain_readiness.py`. The toolchain readiness report is static by default; explicit `local_version_and_platform_probe_only` may run only `rustc --version`, `cargo --version`, `rustup --version`, and `xcode-select -p`, with `xcode-select -p` path output redacted to presence only. It keeps `safe_to_install_toolchain_now=false` and `safe_to_run_cargo_check_now=false`; it still does not install Rust, modify shell profiles, run Cargo/Tauri/package managers, fetch dependencies, generate artifacts, read secrets, read `configs/local/`, capture audio, spawn workers, or call remote providers.

PCWEB-086 adds `code/desktop_tauri/rust-toolchain-installation.policy.json` and `tools/desktop_rust_toolchain_installation_decision.py`. The installation decision report is static and uses `no_install_decision_report_only`: it records the recommended `official_rustup` path, required approval tokens, platform notes, and post-install verification order, but keeps `safe_to_install_toolchain_now=false`, `safe_to_modify_shell_profile_now=false`, `safe_to_run_install_command_now=false`, and `safe_to_run_cargo_check_now=false`. It does not run curl/sh/rustup/cargo/package managers, modify shell profiles, fetch dependencies, generate artifacts, read secrets, read `configs/local/`, capture audio, spawn workers, or call remote providers.

PCWEB-087 adds `code/desktop_tauri/rust-toolchain-install-approval.policy.json` and `tools/desktop_rust_toolchain_install_approval_packet.py`. The install approval packet is static and uses `manual_user_run_only`: it records official Rust/rustup/Tauri source URLs, inert macOS/Linux manual command text, Windows `rustup-init.exe` manual guidance, approval tokens, risk notes, rollback notes, and post-install verification order, while keeping `safe_to_execute_install_now=false`, `safe_to_install_toolchain_now=false`, `safe_to_modify_shell_profile_now=false`, and `safe_to_run_cargo_check_now=false`. It does not execute manual text, run installers/package managers/cargo/Tauri, modify shell profiles or PATH, fetch dependencies, generate artifacts, read secrets, read `configs/local/`, capture audio, spawn workers, or call remote providers.

PCWEB-088 adds `code/desktop_tauri/rust-post-install-probe-approval.policy.json` and `tools/desktop_rust_post_install_probe_approval.py`. The post-install probe approval packet is static and uses `no_probe_execution_approval_packet_only`: it records the future read-only probe allowlist (`rustc --version`, `cargo --version`, `rustup --version`, and macOS `xcode-select -p`), redaction requirements, approval tokens, expected result schema, and cargo-check blockers, while keeping `safe_to_run_post_install_probe_now=false`, `safe_to_run_cargo_check_now=false`, `safe_to_fetch_dependencies_now=false`, and `safe_to_generate_target_dir_now=false`. It does not run probes, inspect PATH/shell profiles/cargo home/rustup home, run cargo/Tauri, generate artifacts, read secrets, read `configs/local/`, capture audio, spawn workers, or call remote providers.

PCWEB-089 adds `code/desktop_tauri/rust-post-install-probe-result-intake.policy.json` and `tools/desktop_rust_post_install_probe_result_intake.py`. The result intake report is static and uses `manual_result_validation_only` with `caller_provided_json_only`: it validates only bounded caller-provided status fields, rejects raw stdout/stderr, command, path, env, home/cache, provider config, api_key, authorization, and bearer token fields, and keeps `safe_to_accept_raw_probe_output_now=false`, `safe_to_run_post_install_probe_now=false`, `safe_to_run_cargo_check_now=false`, and `cargo_check_readiness=blocked_until_pcweb_084_and_user_approval`. It does not run rustc/cargo/rustup/xcode-select, cargo check, Tauri, package managers, read secrets, read `configs/local/`, capture audio, spawn workers, or call remote providers.

PCWEB-090 adds `code/desktop_tauri/first-cargo-check-execution.policy.json` and `tools/desktop_first_cargo_check_execution_boundary.py`. The execution boundary report is static and uses `explicit_manual_execution_packet_only`: it combines the PCWEB-084 command/env/artifact policy with a PCWEB-089 bounded toolchain result and can return `execution_packet_status=ready_for_explicit_user_approval`, but it keeps `safe_to_run_cargo_check_now=false`, `safe_to_fetch_dependencies_now=false`, `safe_to_generate_cargo_lock_now=false`, and `safe_to_generate_target_dir_now=false`. It does not run Cargo, Tauri, package managers, fetch dependencies, generate `Cargo.lock` or target output, read secrets, read `configs/local/`, capture audio, spawn workers, or call remote providers.

PCWEB-091 adds `code/desktop_tauri/tauri-noop-shell-run-smoke.policy.json` and `tools/desktop_tauri_noop_shell_run_smoke.py`. The shell smoke boundary report is static and uses `readiness_report_only`: it validates the PCWEB-082 Tauri scaffold, `devUrl=http://127.0.0.1:8765/`, `frontendDist`, minimal capability, exact no-op command catalog, generated artifact blockers, and the PCWEB-090 no-command boundary. A valid scaffold can only return `smoke_packet_status=ready_for_explicit_tauri_run_approval`; it keeps `safe_to_run_tauri_dev_now=false`, `safe_to_run_cargo_check_now=false`, and `safe_to_capture_audio_now=false`, and it does not run Tauri, Cargo, package managers, capture audio, spawn workers, read secrets, read `configs/local/`, or call remote providers.

DRV-001 adds the workbench Native Runtime panel. In a normal browser it renders `browser_fallback/not_available` and keeps all real-action flags false. In a Tauri WebView it attempts only the three existing PCWEB-082 no-op commands: `runtime_get_status`, `session_prepare`, and `asr_worker_health`. It does not bind audio commands, request permissions, spawn workers, write local files, or call remote providers.

PCWEB-097 adds the workbench ASR handoff dry-run readiness panel. `GET /desktop/asr-worker-handoff-dry-run-readiness` and `desktop-asr-handoff-dry-run-panel` expose PCWEB-096 readiness as `readiness_only` / `preview_only_ready`, including `preview_ready_no_web_mutation`, `explicit_mode_only`, 6 phases, allowed roots, blockers, next decisions, and no-action safety flags. It does not start workers, read event files, mutate Web sessions, capture audio, read real user audio, read `configs/local`, download models, run Cargo/Tauri, or call remote ASR/LLM.

Run local API:

```bash
cd code/web_mvp/backend
PYTHONPATH=.:../../core uvicorn meeting_copilot_web_mvp.app:app --reload --host 127.0.0.1 --port 8765
```

Run local API with JSON persistence:

```bash
cd code/web_mvp/backend
MEETING_COPILOT_DATA_DIR=../../data/local_runtime/web_mvp \
PYTHONPATH=.:../../core uvicorn meeting_copilot_web_mvp.app:app --reload --host 127.0.0.1 --port 8765
```

Run scripted browser E2E smoke:

```bash
cd code/web_mvp
node e2e/browser_smoke.mjs
```

The browser smoke starts the local API with a temporary `MEETING_COPILOT_DATA_DIR`, opens Google Chrome headless, checks `GET /desktop/shell-readiness`, `GET /desktop/runtime-boundary`, `GET /desktop/native-bridge-contract`, and `GET /desktop/asr-worker-handoff-dry-run-readiness`, loads the workbench, verifies `desktop-readiness-panel` renders `blocked_before_desktop_shell`, `8 phases`, and `desktop_safe_to_capture_audio=false`, verifies `desktop-runtime-boundary-panel` renders `blocked_before_runtime_creation`, `tauri_first_electron_fallback`, `sidecar_worker_planned`, `8 phases`, and `desktop_runtime_safe_to_create_shell=false`, verifies `desktop-native-bridge-contract-panel` renders `specified_not_bound`, `runtime.get_status`, `audio.capture_start`, `asr_worker.start`, `8 commands`, and `desktop_bridge_safe_to_create_native_bridge=false`, verifies `desktop-native-runtime-panel` renders browser fallback `not_available` with `safe_to_capture_audio=false`, verifies `desktop-asr-handoff-dry-run-panel` renders `preview_only_ready`, `preview_ready_no_web_mutation`, `explicit_mode_only`, `6 phases`, and false worker/audio/remote safety flags, verifies evidence click-back from a suggestion card to both EvidenceSpan and transcript segment, switches the event panel to Live Mock mode, verifies the browser opens `/live/sessions/{session_id}/events.sse` with `EventSource`, verifies no complete transcript/state/cards/evidence/report are preloaded at stream-open time, waits for `transcript_partial` / `scheduler_event`, verifies transcript/state/cards appear incrementally, consumes a real mock-SSE `transcript_revision` from the `api-review` fixture to verify superseded EvidenceSpan UI, `suggestion_invalidated` audit event, and stale-evidence card muting after cards have already appeared, verifies the finite mock stream is closed after `evaluation_summary`, verifies Live Mock card feedback does not rehydrate the full snapshot or replace the timeline with JSON replay events, switches to Live ASR mode and verifies `/live/asr/sessions/{session_id}/events.sse` can stream local synthetic ASR `partial/final/revision` events into transcript/evidence/state, local deterministic `DecisionCandidate`、`ActionItem`、`Risk` and `OpenQuestion` state candidates, a no-LLM scheduler decision log, no-LLM `suggestion_candidate_event` audit records with local confidence/degradation metadata, no-LLM `llm_request_draft_event` audit records with `draft_only/not_generated/not_created`, a non-formal `/draft.md` review from an empty live view while keeping formal `/report.md` requests at zero, and `PCWEB-078` card lifecycle readiness UI consumption after the Live ASR terminal summary through `card-lifecycle-readiness-panel` and response-only `/llm-card-lifecycle-readiness-summaries`, then switches to the schema degradation fixture and verifies muted cards, `suggestion_silenced` events, and report output. It is a UI/E2E gate for the PC Web MVP; it still does not prove live desktop audio capture, real ASR endpoint finals, real scheduler events, real suggestion card generation, real LLM incremental calls, Tauri/Electron shell creation, native bridge startup in a real Tauri WebView, worker spawn, packaging, signing, or installer creation.

The backend is intentionally a thin local adapter. Core Copilot behavior belongs in `code/core`, and platform-specific capture belongs in later desktop adapters.

Open the local workbench at:

```text
http://127.0.0.1:8765/
```

The workbench uses demo fixtures, core gates, Live Mock SSE, and a synthetic local Live ASR SSE skeleton. It does not prove desktop audio capture, real ASR endpoint finals, real LLM incremental calls, or WebSocket readiness. The Live Mock and Live ASR paths prove the frontend can consume finite local SSE envelopes with browser `EventSource` and apply them to an incremental UI without preloading the full result.

Current workbench scope:

- Load engineering fixtures `api-review`, `release-review`, and `schema-degradation-review`.
- Load non-engineering boundary fixtures `business-sync`, `product-priority`, and `mixed-terms-sync`.
- Show meeting summary and quality metrics.
- Show `PCWEB-079` desktop shell readiness through `desktop-readiness-panel` backed by `GET /desktop/shell-readiness`; this is a template-only, no-capture preflight boundary with `desktop_safe_to_capture_audio=false`, no microphone/system-audio capture, no permission request, no ASR worker start, no provider config read, no browser storage write, and no paid ASR/LLM call.
- Show `PCWEB-080` desktop runtime boundary through `desktop-runtime-boundary-panel` backed by `GET /desktop/runtime-boundary`; this is a decision-preflight boundary with `recommended_desktop_runtime=tauri_first_electron_fallback`, `asr_worker_process_model=sidecar_worker_planned`, `desktop_runtime_safe_to_create_shell=false`, no Tauri/Electron dependency creation, no native bridge startup, no ASR worker spawn, no permission request, no microphone/system-audio capture, no provider config read, no browser storage write, and no paid ASR/LLM call.
- Show `PCWEB-081` desktop native bridge command contract through `desktop-native-bridge-contract-panel` backed by `GET /desktop/native-bridge-contract`; this is a contract-preflight boundary with `desktop_bridge_contract_status=specified_not_bound`, eight command contracts including `runtime.get_status`, `audio.capture_start`, and `asr_worker.start`, error/resource policy metadata, `desktop_bridge_safe_to_create_native_bridge=false`, no Tauri/Electron scaffold, no IPC/native bridge binding, no worker spawn, no permission request, no device enumeration, no microphone/system-audio capture, no provider config read, no local/browser storage write, and no paid ASR/LLM call.
- `PCWEB-082` adds the first static Tauri shell scaffold at `code/desktop_tauri/src-tauri`. Its `tauri.conf.json` uses Tauri v2 keys `devUrl=http://127.0.0.1:8765/` and `frontendDist=../../web_mvp/backend/meeting_copilot_web_mvp/frontend_static`, keeps `beforeDevCommand` and `beforeBuildCommand` empty, and disables bundling for the spike. The scaffold binds only three no-op commands, `runtime_get_status` -> `runtime.get_status`, `session_prepare` -> `session.prepare`, and `asr_worker_health` -> `asr_worker.health`; each returns `command_status=noop_bound`, `implementation_status=noop_only`, `transport_status=tauri_ipc_bound`, and `safe_to_execute_real_action=false`. It still does not bind audio commands, request permissions, enumerate devices, capture microphone/system audio, spawn workers, read provider config or `configs/local/`, write local/browser storage, create `Cargo.lock` or `package.json`, create installers, or call remote ASR/LLM.
- `PCWEB-083` adds desktop build readiness policy through `build-readiness.policy.json` and `desktop_build_readiness.py`. It records `toolchain_version_probe_only`, `safe_to_run_cargo_check_now=false`, future `cargo check --manifest-path code/desktop_tauri/src-tauri/Cargo.toml` preconditions, forbidden build/package commands, forbidden default artifacts, forbidden side effects, and a code-enforced probe allowlist; it does not change the Web workbench runtime.
- `PCWEB-084` adds desktop cargo check artifact policy through `cargo-check.policy.json` and `desktop_cargo_check_policy.py`. It records the future first approved `cargo check`, the repeat `--locked --offline` check, `CARGO_TARGET_DIR=artifacts/tmp/desktop_tauri_target`, Cargo.lock generation/commit policy, network fetch and cleanup boundaries, schema validation, and `safe_to_run_cargo_check_now=false`; it does not change the Web workbench runtime.
- `PCWEB-085` adds desktop Rust toolchain readiness policy through `rust-toolchain-readiness.policy.json` and `desktop_rust_toolchain_readiness.py`. It records `local_version_and_platform_probe_only`, hard-allowlisted toolchain/platform probes, xcode path redaction, first cargo-check readiness blockers, and `safe_to_install_toolchain_now=false`; it does not change the Web workbench runtime.
- Show fixture evaluation summary: engineering gate, effective cards, gap rule coverage, false positives, too_late, kept, schema blocked/silenced counts, and state counts.
- Show state board first: decisions, action items, risks, open questions.
- Startup is passive: it loads desktop readiness, desktop runtime boundary, and the fixture list only, plus the native bridge contract; all four startup reads are no-write/no-capture GET paths, and the workbench does not auto-create a demo session or write local session JSON until the user explicitly loads a fixture. PCWEB-082 adds static desktop source files only; the browser smoke still does not prove a running Tauri shell, real IPC, native bridge startup, worker spawn, audio capture, packaging, signing, notarization, or installer creation.
- Show suggestion cards with trace chips and feedback actions; schema-failed/timeout/invalid cards are muted and not actionable.
- Show replay event timeline generated from the gated snapshot, including derived LLM scheduled/schema-result/silenced trace events.
- Switch the event timeline between Replay and Live Mock mode. Live Mock creates a mock live fixture session, subscribes to `/live/sessions/{session_id}/events.sse` with browser `EventSource`, starts from an empty live view, and applies incoming events to transcript, evidence, state board, suggestion cards, evaluation, report, and the event timeline. `transcript_final` / `transcript_revision` carry `evidence_spans`; revision events also carry `supersedes_segment_id` and `superseded_evidence_spans`, so the frontend can mark old evidence stale/superseded. When that stale evidence affects an existing card, the stream emits `suggestion_invalidated` with the impacted card, stale evidence ids, replacement evidence ids, and triggering revision event; the frontend consumes that payload and mutes the card. Lineage direction is explicit: old evidence uses `replaced_by` to point to the new evidence, and new active evidence uses `revision_of` to point back to the old evidence. `state_event` carries `state_item`, suggestion events carry `card`, and the frontend prefers these payload objects over fixture lookup. Full Markdown report loading is deferred until terminal evaluation summary.
- Switch to Live ASR mode. Live ASR creates a local synthetic ASR event session through `/live/asr/mock/sessions`, subscribes to `/live/asr/sessions/{session_id}/events.sse`, and applies `source=live_asr_stream` events from an empty live view. This validates the ASR runtime streaming contract shape (`partial/final/revision/error/end_of_stream`) at the Web/API boundary. `final/revision` can also emit local deterministic `DecisionCandidate`、`ActionItem`、`Risk` and `OpenQuestion` state candidates, a no-LLM scheduler decision log with `llm_candidate_queued` / `llm_candidate_skipped`, `decision_reason`, cooldown, budget, and `llm_call_status=not_called`, no-LLM `suggestion_candidate_event` audit records with gap rules, local deterministic confidence/degradation metadata, and `card_status=not_created`, plus no-LLM `llm_request_draft_event` audit records that link each candidate to a future LLM request draft while keeping `request_status=draft_only` and `schema_status=not_generated`. The same no-LLM candidate queue is available through read-only `/live/asr/sessions/{session_id}/suggestion-candidates` for later scheduler/card-engine work, and that endpoint intentionally excludes request drafts. The future LLM request draft queue is available through read-only `/live/asr/sessions/{session_id}/llm-request-drafts` for later executor work, and that endpoint intentionally excludes candidates and does not execute requests. `PCWEB-060` OpenAI-compatible request body preview is available through read-only `/live/asr/sessions/{session_id}/llm-openai-request-body-previews`; it derives deterministic chat-completions `messages`、`response_format`、metadata and idempotency keys from request drafts while keeping `body_preview_only/not_called/not_generated/not_created/not_estimated/safe_to_execute=false`, never reading provider config, base URL, API key, authorization headers, keychain, environment secrets, or `configs/local/`, and never calling LLM. `PCWEB-061` adds `local_sensitive_draft_value_guard.v1` to that preview path: draft payload values that match high-risk local markers such as API-key-like tokens, bearer/Authorization markers, provider config markers, `configs/local`, or relay-domain markers are replaced with `[redacted:sensitive_draft_value]` in preview messages/metadata while the original `/llm-request-drafts` and `/events` records remain unchanged for audit. `PCWEB-062` adds an outline-only `SuggestionCardV1` contract to `response_format.json_schema` with `schema_outline_status=outline_only`, required/optional fields, type hints, and `additional_properties_status=allowed_by_local_contract_extra`; it still does not generate a full JSON Schema, validate model responses, create cards, or call LLM. `PCWEB-063` adds `POST /live/asr/sessions/{session_id}/llm-schema-validation-dry-runs`, which validates only caller-provided candidate responses against the local `SuggestionCardV1` dry-run contract, returns passed/failed validation metadata, and still does not generate `llm_schema_result`, create cards, mutate events, or call LLM. A local execution preview is available through read-only `/live/asr/sessions/{session_id}/llm-execution-previews`; it derives idempotency/schema-target metadata from request drafts while keeping `preview_only/not_called/not_generated/not_created/not_estimated`. The executor action boundary is available through `POST /live/asr/sessions/{session_id}/llm-execution-runs` with `mode=disabled`; it returns deterministic `skipped/llm_executor_disabled` runs from those previews and still does not mutate audit events, read credentials, estimate cost, create schema results, create cards, or call LLM. Provider readiness is available through read-only `/live/asr/sessions/{session_id}/llm-provider-readiness`; it reports `not_ready/disabled/not_read/not_called` OpenAI-compatible future contract status without reading local config, environment secrets, or credentials. Provider config boundary is available through read-only `/live/asr/sessions/{session_id}/llm-provider-config-boundary`; it returns template-only field classification/display policy for `base_url`、`api_key`、`model`、`timeout_seconds` and `ca_bundle_path` while not reading config files, not reading environment secrets, not returning configured values, and not calling LLM. Provider masked status is available through read-only `/live/asr/sessions/{session_id}/llm-provider-masked-status`; it returns a template-only future status envelope with null display values and forbidden secret status signals while still not reading config files, not returning real or masked configured values, not reporting key presence/validity/length/hash/prefix/suffix/fingerprint, and not calling LLM. Provider config validation is available through `POST /live/asr/sessions/{session_id}/llm-provider-config-validation`; it validates only the caller-provided OpenAI-compatible config draft, returns safe derived display values, keeps `api_key` null and `safe_to_execute=false`, does not echo secrets in success or 422 responses, and still does not read local config or environment secrets. Provider config loader preflight is available through `POST /live/asr/sessions/{session_id}/llm-provider-config-loader-preflight`; it validates only the future loader request shape and authorization intent, rejects URL/file URL/control-character/traversal paths and duplicate requested fields, returns no raw path, no basename, no parent directory, no path-derived label, keeps `safe_to_load_config=false`, and still does not read local config or environment secrets. Provider secret storage policy is available through read-only `/live/asr/sessions/{session_id}/llm-provider-secret-storage-policy`; it returns a template-only storage policy with recommended secret reference types and forbidden storage locations while not reading config files, environment secrets, keychain, or calling LLM. `PCWEB-058` provider config reader dry-run is available through `POST /live/asr/sessions/{session_id}/llm-provider-config-reader-dry-run`; it validates the authorized future reader request shape, accepts only secret references, rejects URL/file URL/control-character/traversal paths, returns no raw path, basename, parent directory, path-derived label, secret reference id, config existence/readability/size/mtime/hash/fingerprint, API key, authorization value, bearer token, or raw config, keeps `safe_to_read_config=false` and `safe_to_execute=false`, and still does not read config files, environment secrets, keychain, or call LLM. `PCWEB-059` provider masked status loader dry-run is available through `POST /live/asr/sessions/{session_id}/llm-provider-masked-status-loader-dry-run`; it validates the future masked-status loader request shape, requested display fields, secret reference, and explicit authorization flags while keeping config read, secret read, status inference, event mutation, and LLM call disabled; it returns null display values and never returns raw path, basename, parent directory, path-derived label, secret reference id, file existence/readability/size/mtime/hash/fingerprint, API key, masked key, key presence/validity/length/hash/prefix/suffix/fingerprint, authorization value, bearer token, or raw config. After terminal summary, the report panel loads `/live/asr/sessions/{session_id}/draft.md` and displays the non-formal draft warning, state-candidate section, suggestion-candidate section, and LLM request draft section; Live ASR mode does not request the formal `/sessions/{session_id}/report.md` path. It does not load ASR models, read audio files, capture microphone/system audio, call remote ASR, run real state extraction, generate formal suggestion cards, or run LLM analysis.
- `PCWEB-064` adds `POST /live/asr/sessions/{session_id}/llm-card-creation-policy-dry-runs` as the local boundary after schema validation. It validates whether a schema-shaped candidate response is eligible for future card creation by checking request draft linkage, evidence, state events, segment timing, schema show/silence state, and candidate quality while still not reading credentials, calling LLM, generating `llm_schema_result`, creating cards, mutating `/events`, estimating cost, or executing requests.
- `PCWEB-065` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-preview-dry-runs` as the local boundary after card creation policy. It previews whether a future enabled lifecycle would append `llm_schema_result` plus `suggestion_card`, or `llm_schema_result` plus `suggestion_silenced`, while still not reading credentials, calling LLM, generating real lifecycle events, creating cards, mutating `/events`, estimating cost, or executing requests.
- `PCWEB-066` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-preflight-dry-runs` as the local boundary after lifecycle preview. It derives deterministic future event ids, idempotency keys, and would-append sequences, reports conflicts as `append_preflight_status=blocked`, and still does not read credentials, call LLM, write an idempotency store, generate real lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-067` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-runs` as the disabled action boundary after append preflight. It accepts only `mode=disabled`, returns skipped append run envelopes derived from the preflight plan, preserves blocked preflight conflicts, and still does not read credentials, call LLM, write an idempotency store, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-068` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-repository-dry-runs` as the repository append contract dry-run after the disabled action boundary. It accepts only `mode=dry_run_only`, returns repository result envelopes for each future lifecycle event, preserves blocked preflight conflicts, and still does not read credentials, call LLM, write a repository transaction, write an idempotency store, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-069` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-transaction-runs` as the disabled transaction action boundary after the repository dry-run. It accepts only `mode=disabled`, returns skipped transaction run envelopes with `transaction_write_status=disabled`, preserves blocked preflight conflicts, and still does not read credentials, call LLM, commit a repository transaction, write an idempotency store, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-070` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-result-audit-previews` as the append result audit preview after the disabled transaction boundary. It accepts only `mode=preview_only`, returns response-only `card_lifecycle_append_result` audit event previews with `append_result_audit_event_status=preview_only`, preserves blocked preflight conflicts, and still does not read credentials, call LLM, append audit events, commit a repository transaction, write an idempotency store, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-071` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-retry-replay-preflights` as the retry/replay conflict-resolution preflight after the append result audit preview. It accepts only `mode=preflight_only`, returns response-only `retry_replay_preflight_status=analyzed` checks that classify fresh appends, safe same-event replays, partial replays, mismatched event/request/card identities, duplicate idempotency evidence, and idempotency-key conflicts, and still does not read credentials, call LLM, append audit events, commit a repository transaction, write an idempotency store, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-072` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-event-serializer-dry-runs` as the append event serializer dry-run after append preflight. It accepts only `mode=dry_run_only`, returns response-only canonical future `llm_schema_result` plus `suggestion_card` or `suggestion_silenced` event objects with `append_event_serializer_status=serialized`, preserves blocked preflight conflicts, and still does not read credentials, call LLM, append audit events, commit a repository transaction, write an idempotency store, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-073` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-mutation-preflights` as the append mutation preflight after the serializer. It accepts only `mode=preflight_only`, returns response-only `append_mutation_preflight_status=analyzed` checks for the canonical future events, keeps `safe_to_mutate_events=false`, preserves serializer/preflight conflicts, and still does not read credentials, call LLM, append audit events, start or commit a repository transaction, write an idempotency store, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-074` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-transaction-commit-preflights` as the transaction commit preflight after the mutation preflight. It accepts only `mode=preflight_only`, returns response-only `append_transaction_commit_preflight_status=analyzed` checks with `transaction_commit_readiness_status`, treats safe replay as non-mutating replay evidence rather than commit permission, keeps `safe_to_commit_transaction=false`, and still does not read credentials, call LLM, append audit events, begin/commit/rollback a repository transaction, write an idempotency store, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-075` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-idempotency-store-write-preflights` as the idempotency store write preflight after the transaction commit preflight. It accepts only `mode=preflight_only`, returns response-only `idempotency_store_write_preflight_status=analyzed` checks with `idempotency_store_write_readiness_status`, treats safe replay as no-write replay evidence, keeps `safe_to_write_idempotency_store=false`, and still does not read credentials, call LLM, append audit events, begin/commit/rollback a repository transaction, write an idempotency store or marker, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-076` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-append-result-audit-event-persistence-preflights` as the append result audit event persistence preflight after the idempotency store write preflight. It accepts only `mode=preflight_only`, returns response-only `append_result_audit_event_persistence_preflight_status=analyzed` checks with `append_result_audit_event_persistence_readiness_status`, preserves PCWEB-070 audit preview identities, treats safe replay as no-new-audit-persistence evidence, keeps `safe_to_persist_append_result_audit_event=false`, and still does not read credentials, call LLM, append audit events, begin/commit/rollback a repository transaction, write an idempotency store or marker, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-077` adds `POST /live/asr/sessions/{session_id}/llm-card-lifecycle-readiness-summaries` as the UI-facing readiness summary after the append result audit event persistence preflight. It accepts only `mode=summary_only`, returns response-only `card_lifecycle_readiness_summary_status=summarized` with `card_lifecycle_overall_readiness_status`, 12 phase summaries, block reasons, next decisions, and scoped `card_lifecycle_safe_to_*` false flags, treats safe replay as no-new-write replay evidence rather than action permission, and still does not read credentials, call LLM, append audit events, begin/commit/rollback a repository transaction, write an idempotency store or marker, append lifecycle events, create cards, mutate `/events`, estimate cost, or execute requests.
- `PCWEB-078` adds Web workbench consumption of the PCWEB-077 summary. After the Live ASR terminal summary, the frontend derives a local contract probe from the complete accumulated visible no-LLM event stream, posts `mode=summary_only` to `/live/asr/sessions/{session_id}/llm-card-lifecycle-readiness-summaries`, and renders `card-lifecycle-readiness-panel` with overall readiness, 12 phases, block reasons, next decisions, and disabled `not_called/not_appended/not_written` statuses. The response-only probe uses the core-gate allowed `owner_gap` card type, so the UI probe stays compatible with current validation without deciding future formal card taxonomy. It does not render the result as a formal `.suggestion-card`, does not create lifecycle events, does not mutate `/events`, does not read credentials or `configs/local/`, and does not call LLM or remote ASR.
- Show EvidenceSpan and transcript side panels; evidence chips on states and cards click back to both the EvidenceSpan item and its transcript segment.
- Export and refresh Markdown report.
- Delete the current session. In memory mode this removes the in-process record; with `MEETING_COPILOT_DATA_DIR` it removes the session JSON file under the configured local data directory.

Fixture gate boundaries:

- Engineering positive fixtures must pass core gates and cover at least two gap rules. The schema degradation fixture must keep at least one effective card while recording schema failure/timeout/invalid as silenced trace events.
- Non-engineering fixtures must pass with zero engineering suggestion cards.
- `evaluation_summary` is a local replay/value gate. It does not call remote ASR or LLM and does not prove real-time desktop capture quality.
- `/events` and `/events.sse` are replay snapshot timelines. They include `llm_scheduled`, `llm_schema_result`, and `suggestion_silenced` events derived from gated suggestion-card trace fields, so they do not call remote ASR or LLM. Every event carries `source=replay_snapshot` and `trace_kind=replay_derived` to avoid confusing this stream with a live scheduler log. They provide the contract shape for later live SSE/WebSocket work, but they are not yet a true ASR/LLM incremental event source or scheduler event log.
- `/live/mock/fixtures/{fixture_id}/sessions` creates a gated fixture session and emits a mock live event envelope. `/live/sessions/{session_id}/events` and `.sse` expose `transcript_partial`, `transcript_final`, `transcript_revision`, `state_event`, `scheduler_event`, `llm_schema_result`, `suggestion_card` or `suggestion_silenced`, `suggestion_invalidated`, and `evaluation_summary` with `source=live_mock_stream` and `trace_kind=live_event`.
- `/live/asr/mock/sessions` creates an in-process local ASR event session from JSON streaming events. `/live/asr/sessions/{session_id}/events` and `.sse` expose `transcript_partial`, `transcript_final`, `transcript_revision`, local deterministic `state_event` for four PC-1 state lanes, no-LLM scheduler decision `scheduler_event`, no-LLM `suggestion_candidate_event` with candidate quality metadata, no-LLM `llm_request_draft_event`, `provider_error`, and terminal `evaluation_summary` with `source=live_asr_stream` and `trace_kind=live_event`.
- `/live/asr/sessions/{session_id}/suggestion-candidates` reads the same Live ASR audit record and returns only the no-LLM suggestion candidate queue with original event sequence and payload. It is read-only and does not sort, filter, create cards, generate schema results, or call LLM.
- `/live/asr/sessions/{session_id}/llm-request-drafts` reads the same Live ASR audit record and returns only the no-LLM LLM request draft queue with original event sequence and payload. It is read-only and does not sort, filter, execute requests, estimate tokens, create cards, generate schema results, or call LLM.
- `PCWEB-060` `/live/asr/sessions/{session_id}/llm-openai-request-body-previews` reads the same Live ASR audit record and returns deterministic OpenAI-compatible chat-completions request body previews derived from no-LLM request drafts. It is read-only and does not read provider config, base URL, API key, authorization header, keychain, environment secrets, or `configs/local/`; it does not call LLM, estimate tokens, generate full JSON Schema, validate schema, create cards, append events, or execute requests.
- `PCWEB-061` adds `local_sensitive_draft_value_guard.v1` to `/live/asr/sessions/{session_id}/llm-openai-request-body-previews`. The guard replaces high-risk draft payload strings with `[redacted:sensitive_draft_value]` before they enter preview messages or metadata, returns redaction audit fields, and does not mutate `/events` or `/llm-request-drafts`.
- `PCWEB-062` adds an outline-only `SuggestionCardV1` schema contract to `/live/asr/sessions/{session_id}/llm-openai-request-body-previews` under `response_format.json_schema`. It exposes `schema_outline_status=outline_only`, `schema_outline_source=local_contract_preview`, required/optional fields, type hints, and `additional_properties_status=allowed_by_local_contract_extra` without generating a full JSON Schema, validating model responses, creating cards, mutating `/events`, or calling LLM.
- `PCWEB-063` `/live/asr/sessions/{session_id}/llm-schema-validation-dry-runs` validates only caller-provided candidate responses against the local `SuggestionCardV1` dry-run contract. It requires string `mode/request_id` and true JSON integers for token/timing fields, returning passed/failed validation metadata and request linkage without reading credentials, calling LLM, generating `llm_schema_result`, creating cards, mutating `/events`, estimating cost, or executing requests.
- `PCWEB-064` `/live/asr/sessions/{session_id}/llm-card-creation-policy-dry-runs` validates only caller-provided schema-shaped candidate responses against the local card creation policy. It checks request draft linkage, evidence, state events, segment timing, schema show/silence state, and candidate quality, returning allowed/blocked dry-run status while still not reading credentials, calling LLM, generating `llm_schema_result`, creating cards, mutating `/events`, estimating cost, or executing requests.
- `PCWEB-065` `/live/asr/sessions/{session_id}/llm-card-lifecycle-preview-dry-runs` is available as a dry-run lifecycle preview. It reuses schema validation and card creation policy, then returns preview-only future `llm_schema_result` plus `suggestion_card` or `suggestion_silenced` events while keeping `safe_to_append_events=false`, `safe_to_create_card=false`, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-066` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-preflight-dry-runs` is available as a dry-run append preflight. It reuses lifecycle preview, then returns response-only future event ids, idempotency keys, and would-append sequence placement while keeping `safe_to_append_events=false`, no idempotency store write, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-067` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-runs` is available as a disabled append-run boundary. It accepts only `mode=disabled`, maps the PCWEB-066 append plan into skipped runs with `event_append_status=not_appended` and `idempotency_store_status=not_written`, and keeps `safe_to_append_events=false`, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-068` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-repository-dry-runs` is available as a repository append contract dry-run. It accepts only `mode=dry_run_only`, maps the PCWEB-067 skipped runs into repository result envelopes with `repository_write_status=dry_run_only`, `event_append_status=not_appended`, and `idempotency_store_status=not_written`, preserves preflight conflicts as `blocked_by_preflight`, and keeps `safe_to_append_events=false`, no repository transaction, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-069` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-transaction-runs` is available as a disabled transaction-run boundary. It accepts only `mode=disabled`, maps the PCWEB-068 repository result envelopes into skipped transaction runs with `transaction_write_status=disabled`, `repository_transaction_status=disabled`, `event_append_status=not_appended`, and `idempotency_store_write_status=not_written`, preserves preflight conflicts as `repository_preflight_blocked`, and keeps `safe_to_commit_transaction=false`, `safe_to_append_events=false`, no repository transaction commit, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-070` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-result-audit-previews` is available as an append result audit preview boundary. It accepts only `mode=preview_only`, maps the PCWEB-069 skipped transaction runs into response-only audit event previews with `append_result_audit_event_status=preview_only`, `audit_event_append_status=not_appended`, and `safe_to_write_audit_events=false`, preserves preflight conflicts as `blocked_by_preflight`, and keeps no audit event append, no repository transaction commit, no idempotency-store write, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-071` `/live/asr/sessions/{session_id}/llm-card-lifecycle-retry-replay-preflights` is available as a retry/replay preflight boundary. It accepts only `mode=preflight_only`, maps the PCWEB-070 audit preview items into response-only retry/replay checks with `retry_replay_preflight_status=analyzed`, classifies `no_existing_append`、`safe_replay_same_event`、`blocked_mismatched_replay`、`blocked_existing_idempotency_key` and partial replay states, requires safe same-event replay to match request/draft/card identity, blocks duplicate append idempotency evidence, and keeps no event mutation, no repository transaction commit, no idempotency-store write, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-072` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-event-serializer-dry-runs` is available as an append event serializer dry-run boundary. It accepts only `mode=dry_run_only`, maps the PCWEB-065/066 preview and append plan into canonical future persisted event objects with `append_event_serializer_status=serialized`, `event_append_status=not_appended`, `idempotency_store_status=not_written`, and `idempotency_store_write_status=not_written`, preserves preflight conflicts as `blocked_by_preflight`, and keeps no event mutation, no repository transaction commit, no idempotency-store write, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-073` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-mutation-preflights` is available as an append mutation preflight boundary. It accepts only `mode=preflight_only`, maps PCWEB-072 serialized events into response-only mutation preflight checks with `append_mutation_preflight_status=analyzed`, `repository_transaction_status=not_started`, `event_append_status=not_appended`, `idempotency_store_status=not_written`, `idempotency_store_write_status=not_written`, and `safe_to_mutate_events=false`, preserves serializer/preflight conflicts as `blocked_by_serializer_preflight`, and keeps no event mutation, no repository transaction commit, no idempotency-store write, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-074` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-transaction-commit-preflights` is available as a transaction commit preflight boundary. It accepts only `mode=preflight_only`, maps PCWEB-073 mutation checks and PCWEB-071 retry/replay checks into response-only commit readiness checks with `append_transaction_commit_preflight_status=analyzed`, `transaction_commit_readiness_status`, `repository_transaction_commit_status=not_committed`, `event_append_status=not_appended`, `idempotency_store_write_status=not_written`, and `safe_to_commit_transaction=false`, preserves partial replay and retry/replay conflicts, and keeps no event mutation, no repository transaction begin/commit/rollback, no idempotency-store write, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-075` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-idempotency-store-write-preflights` is available as an idempotency store write preflight boundary. It accepts only `mode=preflight_only`, maps PCWEB-074 commit readiness checks into response-only idempotency store write checks with `idempotency_store_write_preflight_status=analyzed`, `idempotency_store_write_readiness_status`, `idempotency_store_write_status=not_written`, and `safe_to_write_idempotency_store=false`, distinguishes fresh would-write-if-enabled records from safe replay no-write evidence, preserves partial replay and retry/replay conflicts, and keeps no event mutation, no repository transaction begin/commit/rollback, no idempotency-store write or marker, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-076` `/live/asr/sessions/{session_id}/llm-card-lifecycle-append-result-audit-event-persistence-preflights` is available as an append result audit event persistence preflight boundary. It accepts only `mode=preflight_only`, maps PCWEB-075 idempotency-store write checks into response-only audit persistence checks with `append_result_audit_event_persistence_preflight_status=analyzed`, `append_result_audit_event_persistence_readiness_status`, `audit_event_append_status=not_appended`, and `safe_to_persist_append_result_audit_event=false`, preserves PCWEB-070 `audit_event_id` and `audit_idempotency_key`, distinguishes fresh would-persist-if-enabled audit events from safe replay no-persistence evidence, preserves partial replay, retry/replay conflicts, and upstream idempotency-store blockers, and keeps no audit event append, no event mutation, no repository transaction begin/commit/rollback, no idempotency-store write or marker, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-077` `/live/asr/sessions/{session_id}/llm-card-lifecycle-readiness-summaries` is available as a card lifecycle readiness summary boundary. It accepts only `mode=summary_only`, maps PCWEB-076 source readiness into response-only `card_lifecycle_readiness_summary_status=summarized`, `card_lifecycle_overall_readiness_status`, `source_preflight_status=analyzed`, `source_readiness_status`, 12 PCWEB-065..076 phase summaries, `card_lifecycle_block_reasons`, and `card_lifecycle_next_required_decisions`, avoids leaking upstream true replay `safe_to_*` action fields, keeps `card_lifecycle_safe_to_execute_llm=false`, `card_lifecycle_safe_to_create_card=false`, `card_lifecycle_safe_to_append_events=false`, `card_lifecycle_safe_to_write_idempotency_store=false`, and `card_lifecycle_safe_to_persist_append_result_audit_event=false`, and keeps no audit event append, no event mutation, no repository transaction begin/commit/rollback, no idempotency-store write or marker, no credential reads, no LLM calls, and no `/events` mutation.
- `PCWEB-078` renders that readiness summary inside the Web workbench after Live ASR reaches terminal `evaluation_summary`. The UI uses a local contract probe from already displayed request draft/evidence/state events, shows `blocked_until_enabled` and `12 phases` when the current disabled chain is healthy, and keeps the Live ASR formal suggestion card count at zero.
- `PCWEB-079` adds `GET /desktop/shell-readiness` and renders its response in `desktop-readiness-panel` at workbench startup. The endpoint returns `desktop_readiness_status=blocked_before_desktop_shell`, `desktop_readiness_phase_count=8`, blockers, next decisions, and scoped flags such as `desktop_safe_to_capture_audio=false`; it is a read-only desktop-entry boundary and does not capture microphone/system audio, request permissions, load ASR models, start workers, read provider config or `configs/local/`, write local/browser storage, create installers, or call remote ASR/LLM.
- `PCWEB-080` adds `GET /desktop/runtime-boundary` and renders its response in `desktop-runtime-boundary-panel` at workbench startup and reset. The endpoint returns `desktop_runtime_boundary_status=blocked_before_runtime_creation`, `desktop_runtime_phase_count=8`, `recommended_desktop_runtime=tauri_first_electron_fallback`, `asr_worker_process_model=sidecar_worker_planned`, blockers, next decisions, and scoped flags such as `desktop_runtime_safe_to_create_shell=false`; it is a read-only runtime decision boundary and does not create Tauri/Electron project files, install dependencies, start a native bridge, spawn workers, request permissions, capture microphone/system audio, read provider config or `configs/local/`, write local/browser storage, create installers, or call remote ASR/LLM.
- `PCWEB-081` adds `GET /desktop/native-bridge-contract` and renders its response in `desktop-native-bridge-contract-panel` at workbench startup and reset. The endpoint returns `desktop_bridge_contract_status=specified_not_bound`, `desktop_bridge_command_count=8`, `desktop_bridge_phase_count=8`, a response-only command catalog (`runtime.get_status`, `session.prepare`, `audio.permissions_status`, `audio.devices_list`, `audio.capture_start`, `audio.capture_stop`, `asr_worker.start`, `asr_worker.health`), an error envelope contract, a resource policy contract, blockers, next decisions, and scoped flags such as `desktop_bridge_safe_to_create_native_bridge=false`; it is the last bridge/process preflight before a real Tauri shell spike, and the next desktop increment must be `create_tauri_shell_scaffold_against_bridge_contract`. It does not create Tauri/Electron project files, install dependencies, bind IPC, start a native bridge, spawn workers, request permissions, enumerate audio devices, capture microphone/system audio, read provider config or `configs/local/`, write local/browser storage, create installers, or call remote ASR/LLM.
- `PCWEB-082` creates `code/desktop_tauri/src-tauri` as a static desktop source scaffold against that bridge contract. It includes `Cargo.toml`, `build.rs`, `tauri.conf.json`, `capabilities/default.json`, `src/main.rs`, and `src/lib.rs`, and binds only `runtime_get_status`, `session_prepare`, and `asr_worker_health` as no-op handlers with `noop_bound` and `safe_to_execute_real_action=false`. This is not yet a running desktop app, real IPC bridge, audio adapter, ASR sidecar worker, installer, signing, or notarization path.
- `PCWEB-083` keeps the same no-runtime stance while adding a tested build-readiness boundary. `desktop_build_readiness.py` can produce a static policy report, or an explicit `toolchain_version_probe_only` report, but it must keep `safe_to_run_cargo_check_now=false` until lockfile, target directory, dependency fetch, cleanup, and no-audio/no-secret/no-remote preconditions are approved.
- `PCWEB-084` keeps the same no-run stance while deciding the cargo check artifact policy. `desktop_cargo_check_policy.py` produces only a static/read-only report, keeps `safe_to_run_cargo_check_now=false`, and requires `CARGO_TARGET_DIR=artifacts/tmp/desktop_tauri_target` for any future approved check.
- `PCWEB-085` keeps the same no-install/no-build stance while adding a toolchain readiness preflight. `desktop_rust_toolchain_readiness.py` is static by default and optional probe mode remains `local_version_and_platform_probe_only`, with `safe_to_install_toolchain_now=false` and `safe_to_run_cargo_check_now=false`.
- `PCWEB-086` keeps the same no-install/no-build stance while adding the Rust toolchain installation decision preflight. `desktop_rust_toolchain_installation_decision.py` is static only, `no_install_decision_report_only`, and keeps `safe_to_install_toolchain_now=false`, `safe_to_run_install_command_now=false`, and `safe_to_run_cargo_check_now=false`.
- `PCWEB-087` keeps the same no-install/no-build stance while adding the Rust toolchain install approval packet. `desktop_rust_toolchain_install_approval_packet.py` is static only, `manual_user_run_only`, and keeps `safe_to_execute_install_now=false`, `safe_to_install_toolchain_now=false`, and `safe_to_run_cargo_check_now=false`.
- `PCWEB-088` keeps the same no-probe/no-build stance while adding the Rust post-install probe approval packet. `desktop_rust_post_install_probe_approval.py` is static only, `no_probe_execution_approval_packet_only`, and keeps `safe_to_run_post_install_probe_now=false` and `safe_to_run_cargo_check_now=false`.
- `PCWEB-089` adds the no-command Rust post-install probe result intake. `desktop_rust_post_install_probe_result_intake.py` is static only, `manual_result_validation_only`, accepts `caller_provided_json_only`, rejects raw/path/secret-bearing result fields, and keeps `safe_to_accept_raw_probe_output_now=false` and `safe_to_run_cargo_check_now=false` with `blocked_until_pcweb_084_and_user_approval`.
- `PCWEB-090` adds the no-command first cargo check execution boundary. `desktop_first_cargo_check_execution_boundary.py` is static only, `explicit_manual_execution_packet_only`, validates PCWEB-084 and PCWEB-089 inputs, can generate only a manual packet with `ready_for_explicit_user_approval`, and keeps `safe_to_run_cargo_check_now=false`.
- `PCWEB-095` and `PCWEB-096` connect the desktop ASR worker handoff contract to this Web API without starting desktop runtime. `desktop_asr_worker_handoff_preflight.py` validates a future worker descriptor and creates a `/live/asr/local-event-files/sessions` request preview; `desktop_asr_worker_handoff_local_dry_run.py` defaults to `preview_only` and only in explicit `synthetic_local_test` mode calls this Web API through FastAPI `TestClient` with a synthetic event file and temp data dir. They do not start an ASR worker, access microphone/system audio, read real user audio, read `configs/local`, download models, or call remote ASR/LLM.
- `PCWEB-097` exposes that handoff dry-run readiness in the Web workbench through `GET /desktop/asr-worker-handoff-dry-run-readiness` and `desktop-asr-handoff-dry-run-panel`. This is still a no-action readiness view: it does not read event files, mutate Live ASR sessions, start workers, access audio, read real user audio, read `configs/local`, download models, run Cargo/Tauri, or call remote providers.
- `/live/asr/sessions/{session_id}/llm-execution-previews` reads the same Live ASR audit record and returns only local execution previews derived from no-LLM request drafts. It is read-only and does not mutate audit events, execute requests, read credentials, estimate tokens, create cards, generate schema results, or call LLM.
- `/live/asr/sessions/{session_id}/llm-execution-runs` is the local executor entrypoint, but currently accepts only `mode=disabled`. It returns skipped run envelopes derived from execution previews and does not mutate audit events, read credentials, execute requests, estimate tokens, create cards, generate schema results, or call LLM.
- `/live/asr/sessions/{session_id}/llm-provider-readiness` reports the future OpenAI-compatible provider contract as `not_ready` while keeping `provider_config_source=not_read` and `credentials_status=not_read`. It is read-only and does not read `configs/local/`, environment secrets, provider config files, execute requests, estimate tokens, create cards, generate schema results, or call LLM.
- `/live/asr/sessions/{session_id}/llm-provider-config-boundary` reports a template-only provider config secret/display policy for future OpenAI-compatible config. It may list field names such as `api_key` as metadata, but it never reads config files or environment secrets, never returns configured values, never reports key presence/validity/length/hash/prefix/suffix/fingerprint, never mutates audit events, and never calls LLM.
- `/live/asr/sessions/{session_id}/llm-provider-masked-status` reports a template-only future masked provider status envelope. It returns null display values for `base_url_origin`, `model`, `timeout_seconds`, `ca_bundle_name`, and `api_key`; it never reads config files or environment secrets, never returns real or masked configured values, never reports key presence/validity/length/hash/prefix/suffix/fingerprint, never mutates audit events, and never calls LLM.
- `/live/asr/sessions/{session_id}/llm-provider-config-validation` validates only the caller-provided provider config draft request body. It may derive `base_url_origin`, `model`, `timeout_seconds`, and a CA bundle basename, but it never returns or masks the submitted `api_key`, never reports key presence/validity/length/hash/prefix/suffix/fingerprint, never reads config files or environment secrets, never mutates audit events, and never calls LLM.
- `/live/asr/sessions/{session_id}/llm-provider-config-loader-preflight` validates only a future provider config loader request shape and authorization intent. It rejects URL/file URL/control-character/traversal paths and duplicate requested fields, never reads config files or environment secrets, never checks file existence/readability/size/mtime/hash/fingerprint, never returns the raw submitted path, basename, parent directory, or path-derived label, never mutates audit events, and never calls LLM.
- `PCWEB-057` `/live/asr/sessions/{session_id}/llm-provider-secret-storage-policy` reports a template-only provider secret storage policy. It recommends secret references such as OS keychain, enterprise secret provider, or development-only env var name references; it never reads config files, environment secrets, keychain entries, or credential values, never reports key presence/validity/length/hash/prefix/suffix/fingerprint, never mutates audit events, and never calls LLM.
- `PCWEB-058` `/live/asr/sessions/{session_id}/llm-provider-config-reader-dry-run` validates only the future authorized provider config reader request shape. It requires `reader_mode=dry_run_only`, an OpenAI-compatible protocol, a local path reference, a secret reference, and explicit authorization flags that keep config read, secret read, LLM call, and event mutation disabled; it never reads config files or environment secrets, never resolves keychain or secret references, never checks file existence/readability/size/mtime/hash/fingerprint, never returns raw path, basename, parent directory, path-derived labels, secret reference id, API key, authorization values, bearer token, raw config, or masked secret signals, never mutates audit events, and never calls LLM.
- `PCWEB-059` `/live/asr/sessions/{session_id}/llm-provider-masked-status-loader-dry-run` validates only the future authorized masked status loader request shape. It requires `loader_mode=masked_status_dry_run_only`, an OpenAI-compatible protocol, a local path reference, a secret reference, requested display fields, and explicit authorization flags that keep config read, secret read, LLM call, event mutation, and status-value inference disabled; it returns null display values, never reads config files or environment secrets, never resolves keychain or secret references, never checks file existence/readability/size/mtime/hash/fingerprint, never returns raw path, basename, parent directory, path-derived labels, secret reference id, API key, masked API key, key presence/validity/length/hash/prefix/suffix/fingerprint, authorization values, bearer token, or raw config, never mutates audit events, and never calls LLM.
- `/live/asr/sessions/{session_id}/draft` and `/live/asr/sessions/{session_id}/draft.md` derive a local draft review from the Live ASR audit record. The draft includes transcript segments, EvidenceSpans, local state candidates, no-LLM scheduler decisions, no-LLM suggestion candidates with local candidate quality metadata, no-LLM LLM request drafts, provider errors, and stream summary. It is explicitly marked `review_type=asr_live_draft`, `is_formal_report=false`, and `llm_call_status=not_called`; it is not a formal gated report and does not create suggestion cards. The Web workbench displays this markdown draft in Live ASR mode after `evaluation_summary` and when refreshing the report panel, while keeping the formal report endpoint unused for Live ASR sessions.
- Mock live events do not call remote ASR or LLM. They are a contract skeleton for the future desktop ASR/LLM incremental source; they do not prove microphone/system-audio capture, provider endpoint final quality, real scheduler execution, real LLM incremental calls, or WebSocket transport. The current workbench consumes the mock live SSE envelope through `EventSource`; transcript final/revision evidence, revision evidence lifecycle, state events, and card events are self-contained enough for UI rendering, while report still uses gated session context. Revision currently mutates evidence lifecycle, emits a card invalidation audit event, and updates UI actionability only; real state recompute, card regeneration, and durable repository revocation remain future work.
- Live ASR events are a local skeleton for ASR worker integration. They prove event envelope shape, SSE transport, partial/final/revision EvidenceSpan rendering, local deterministic `DecisionCandidate` / `ActionItem` / `Risk` / `OpenQuestion` extraction, local scheduler/candidate/request-draft audit consumption, source separation from `live_mock_stream`, and optional local JSON audit persistence; they do not prove desktop audio capture, model quality, provider endpoint final timing, production state extraction, suggestion card generation, or LLM usage.
- JSON persistence is a PC-1 bridge before the Mac desktop shell. It persists session snapshots, card feedback, and Live ASR JSON audit records under `live_asr_sessions/` when `MEETING_COPILOT_DATA_DIR` or `create_app(data_dir=...)` is set; raw audio files, audio chunks, exported files, and model caches will be connected to the desktop data lifecycle later.
