# Pi 实时教练 Agent 验收目标

## 本轮目标

验证用户能在真实工作台中看见一条完整的 Agent 闭环：稳定转写进入后，Pi 使用场景技能包完成检查，必要时给出一条带原文证据的可执行建议；不需要打断时明确显示静默原因。验收同时记录录音状态、运行时、Agent turn、工具调用、历史检索和决策延迟。

## 可量化门禁

- 每个干预必须带至少一个 `evidence_segment_id`，目标通过率 100%。证据原文由 Pi bridge 和 Python 侧共同校验，不能引用不存在或改写的文本。
- 每个静默结果必须带 `decision_reason`，目标通过率 100%。页面要区分“Pi 正在工作但选择静默”和“没有录音/没有触发”。
- 同一会议中相同事件和证据不得重复出现，目标重复数为 0。历史建议保留在列表中，但当前卡片只突出最新有效建议。
- 决策延迟分两级：产品目标 P95 <= 5 秒，硬上限 <= 10 秒。超过目标要进入性能问题清单，不能继续宣称实时低延迟。
- Pi 请求成功时 `runtime_used=pi` 且无 fallback；Pi 失败时必须可见地标识 fallback 或静默保护，不能伪装成已监听。

## 当前真实页面结果

工作台：`http://127.0.0.1:8766/workbench?meeting_id=pi-interview-real-provider-20260818-002`

- 已确认真实 Provider 的 Pi 运行成功，页面展示场景技能包、当前建议、原文依据、历史建议和本轮检查/延迟/上下文状态。
- 已观察到两轮 Pi 结果，延迟约 6.76 秒和 8.76 秒；因此暂未达到 P95 5 秒目标，但仍在 10 秒硬上限内。
- 当前真实麦克风检查返回“麦克风权限被拒绝”。左侧已有文字来自此前的稳定文本验收注入，不代表当前麦克风正在采集；本次不能把它计为真实音频闭环通过。

## 下一步实现顺序

1. 在允许麦克风访问后，用同一页面采集至少 3 段自然语音，逐段记录“新转写 -> Pi 状态 -> 建议/静默 -> 延迟”。
2. 用 `tools/realtime_coach_eval/replay.py` 对每个场景技能包跑 direct/Pi 对照，报告干预精确率、静默准确率、证据覆盖率、重复数和 P95。
3. 如果 P95 仍超过 5 秒，先缩短上下文和模型输出预算，再评估是否需要把“检查”和“建议生成”拆成快速门控与异步增强两级。
